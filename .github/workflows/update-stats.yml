name: Update GitHub Stats

on:
  schedule:
    - cron: "0 */8 * * *"   # runs every 8 hours automatically
  workflow_dispatch:          # lets you run it manually anytime
  push:
    branches: [main]

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.PAT_1 }}

      - name: Update stats cards cache
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.PAT_1 }}
          script: |
            console.log('Stats token active — private repos and org data included');
